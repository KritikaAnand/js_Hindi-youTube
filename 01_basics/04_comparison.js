console.log(2 > 1);

//when you compare make sure that both having same datatype

console.log(null > 0);   //false
console.log(null == 0);  //false
console.log(null >= 0);  //true

// === it checks the values strictly as well as the datatypes

console.log("2" === 2);
